# ==============================================
# GPTees Environment Variables
# ==============================================
#
# This project requires separate environment configuration for:
# - Backend (Node.js/Express): See backend/.env.example
# - Frontend (React/Vite): See frontend/.env.example
#
# Copy the respective .env.example files to .env and fill in your values:
#   cd backend && cp .env.example .env
#   cd frontend && cp .env.example .env
#
# For detailed setup instructions, see README.md
